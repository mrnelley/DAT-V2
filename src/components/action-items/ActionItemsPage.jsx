import PlaceholderPage from '../shared/PlaceholderPage';

export default function ActionItemsPage() {
  return (
    <PlaceholderPage
      eyebrow="Module"
      title="Action Items"
      description="Personal and team task list with checkbox-complete optimistic UI, owner avatars, due-date chips, and priority links."
      tags={['Task List', 'Optimistic Complete', 'Filters']}
    />
  );
}
