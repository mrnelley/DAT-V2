import PlaceholderPage from '../shared/PlaceholderPage';

export default function PrioritiesPage() {
  return (
    <PlaceholderPage
      eyebrow="Module"
      title="Manage Priorities"
      description="Quarter selector, filter panel, priority rows with drag-reorder, expand-to-heatmap, and the right-side Edit Priority Panel."
      tags={['Priority Rows', 'Heatmap', 'Graph', 'Edit Panel']}
    />
  );
}
