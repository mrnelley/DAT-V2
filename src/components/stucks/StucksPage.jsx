import PlaceholderPage from '../shared/PlaceholderPage';

export default function StucksPage() {
  return (
    <PlaceholderPage
      eyebrow="Module"
      title="Manage Stucks"
      description="Two-section view: Things I Am Holding Up and Things I Am Stuck On. Add Stuck modal with optimistic UI."
      tags={['Tables', 'Add Stuck Modal', 'Optimistic UI']}
    />
  );
}
