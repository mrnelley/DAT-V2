import PlaceholderPage from '../shared/PlaceholderPage';

export default function DataTablePage() {
  return (
    <PlaceholderPage
      eyebrow="Module"
      title="My Data Table"
      description="Period tabs (Weekly / Monthly / Quarterly / Period / Annual), grouped collapsible rows with metric values per period."
      tags={['Period Tabs', 'Grouped Rows', 'Search']}
    />
  );
}
