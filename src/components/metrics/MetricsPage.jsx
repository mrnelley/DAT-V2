import PlaceholderPage from '../shared/PlaceholderPage';

export default function MetricsPage() {
  return (
    <PlaceholderPage
      eyebrow="Module"
      title="Metrics"
      description="Manage KPIs and metric definitions. Connects to Bulk Create modal and Data Table view."
      tags={['Metric Library', 'Connections', 'Bulk Create']}
    />
  );
}
